let namearr = ['Steven', 'Bryan']

function nameCheck(name) {
  for (var i = 0; i < arguments.length; i++) {
    if (i === name) {
      console.log(`What is up ${name}`)
    } else {
      console.log(`What is up ${name}`)
    }
  } i++
}

nameCheck('Steven')